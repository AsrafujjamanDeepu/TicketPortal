// Production environment. Replace apiBaseUrl with the deployed backend URL —
// swapped in automatically by the production build configuration.
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.ticketportal.example.com/api',
  appName: 'TicketPortal',
};
